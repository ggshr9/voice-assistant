# 实时字幕翻译 实现计划

> **For agentic workers:** 用 superpowers:subagent-driven-development 或 executing-plans 逐任务实现。步骤用 `- [ ]` 勾选。
> **注意:本项目不是 git 仓库** —— 计划里没有 commit 步骤,每个任务以"可独立验证的产物"收尾(跑测试/手动验证)。

**Goal:** 开会时把对方说的外语实时转写并翻成中文,显示为屏幕底部双语字幕条;本地、通用、不抢麦。

**Architecture:** 系统输出切到 `会议外放`(扬声器+BlackHole) → 采集 BlackHole → webrtcvad 按停顿切句 → 每句送 whisper(8082)转外语原文 → Qwen3.6(8080)译中文 → pyobjc 浮窗底部双语字幕条。字幕是独立进程(自带 NSApplication GUI 循环),与录音/纪要互不干扰。

**Tech Stack:** Python(uv 内联依赖)、sounddevice、webrtcvad、numpy、pyobjc(AppKit)、mlx-whisper(复用 stt_server)、Qwen3.6(8080)、SwitchAudioSource。

## Global Constraints
- 全本地;不联网。
- 方向固定:外语 → 中文。只转写远端(BlackHole),不录麦、不出纪要。
- 复用现有服务:STT=`stt_server.py`@8082,翻译=Qwen3.6@8080。
- 启动/停止/异常都必须 `try/finally` 把系统输出切回原设备。
- 复用 chat.py 的常量:`SAMPLE_RATE=16000` `FRAME_MS=30` `FRAME=480` `SILENCE_TAIL=0.8` `MIN_SPEECH=0.4`,以及 `HALLU_PHRASES`/`is_hallucination`。
- 跨线程更新 GUI 必须回主线程(后台线程建的 NSTimer 不触发——菜单栏 app 已踩过)。
- 设备名常量:输入 `BlackHole 2ch`、输出多输出 `会议外放`、兜底输出 `MacBook Pro Speakers`。

---

## 文件结构
- 改 `~/voice-assistant/stt_server.py` — 支持语种自动检测 + 返回检测到的语种(向后兼容)。
- 建 `~/voice-assistant/caption_overlay.py` — pyobjc 底部双语字幕浮窗(show/update/hide)。
- 建 `~/voice-assistant/caption_core.py` — 纯逻辑:VAD 切句、STT 调用、翻译调用、幻觉过滤(可单测,不碰 GUI/音频设备)。
- 建 `~/voice-assistant/caption.py` — 编排器:NSApplication + 采集线程 + 处理线程 + 主线程轮询刷新浮窗 + 输出切换。
- 建 `~/.local/bin/caption` — CLI 启动器(检服务→uv run caption.py)。
- 改 `~/voice-assistant/meeting_app.py` — 菜单加「🌐 实时字幕」开/关(经 launchd 启停 caption 进程)。

---

## Task 1: stt_server 支持语种自动检测 + 返回语种

**Files:**
- Modify: `~/voice-assistant/stt_server.py`
- Test: `~/voice-assistant/tests/test_stt_lang.py`(新建)

**Interfaces:**
- Produces: `POST /transcribe {path, language?}` → `{text, language}`。`language` 省略或为 `"auto"`/`null` 时 whisper 自动检测;返回里始终带检测到的 `language`(如 `"en"`/`"zh"`)。向后兼容:传 `language:"zh"` 仍强制中文。

- [ ] **Step 1: 写失败测试**

```python
# ~/voice-assistant/tests/test_stt_lang.py
# /// script
# requires-python = ">=3.10"
# dependencies = ["requests"]
# ///
import subprocess, requests, os

def _say(text, path):
    subprocess.run(["say", "-o", path, "--data-format=LEF32@16000", text], check=True)

def test_autodetect_english():
    p = "/tmp/cap_en.wav"; _say("Let us review the third quarter roadmap.", p)
    r = requests.post("http://127.0.0.1:8082/transcribe",
                      json={"path": p, "language": "auto"}, timeout=30).json()
    assert "text" in r and r["text"].strip(), r
    assert r.get("language") == "en", r          # 自动检测到英语
    assert "roadmap" in r["text"].lower() or "review" in r["text"].lower(), r

def test_backward_compat_zh():
    p = "/tmp/cap_zh.wav"; _say("今天我们过一下路线图", p)
    r = requests.post("http://127.0.0.1:8082/transcribe",
                      json={"path": p, "language": "zh"}, timeout=30).json()
    assert "language" in r, r

if __name__ == "__main__":
    test_autodetect_english(); test_backward_compat_zh(); print("OK")
```

- [ ] **Step 2: 跑测试确认失败**

先确保服务在跑:`pkill -f stt_server; (cd ~/voice-assistant && nohup uv run stt_server.py >/tmp/stt.log 2>&1 &)`,等 `✅ STT 服务就绪`。
Run: `cd ~/voice-assistant && uv run tests/test_stt_lang.py`
Expected: FAIL — 当前响应没有 `language` 字段(KeyError/assert)。

- [ ] **Step 3: 改 stt_server.py**

把 `transcribe()` 改成:

```python
@app.post("/transcribe")
def transcribe():
    path = request.json.get("path")
    lang = request.json.get("language", "zh")
    if lang in (None, "auto", ""):
        lang = None                      # None → whisper 自动检测
    try:
        r = mlx_whisper.transcribe(
            path, path_or_hf_repo=MODEL, language=lang,
            condition_on_previous_text=False,
        )
        return jsonify({"text": r["text"].strip(),
                        "language": r.get("language", lang or "")})
    except Exception as e:
        return jsonify({"error": str(e)}), 500
```

- [ ] **Step 4: 重启服务并跑测试**

Run: `pkill -f stt_server; (cd ~/voice-assistant && nohup uv run stt_server.py >/tmp/stt.log 2>&1 &)` 等就绪,再 `cd ~/voice-assistant && uv run tests/test_stt_lang.py`
Expected: PASS（打印 OK）。

---

## Task 2: caption_core — 纯逻辑(VAD 切句 / STT / 翻译 / 过滤)

**Files:**
- Create: `~/voice-assistant/caption_core.py`
- Test: `~/voice-assistant/tests/test_caption_core.py`

**Interfaces:**
- Produces:
  - `segment_frames(frames, vad, sample_rate=16000, frame_ms=30, silence_tail=0.8, min_speech=0.4) -> iterator[bytes]`:输入 30ms int16 PCM 帧的可迭代,产出每个语音段的拼接 PCM bytes(已按停顿切句、丢弃过短段)。
  - `pcm_to_wav(pcm: bytes, path: str, sample_rate=16000)`:写单声道 16bit wav。
  - `stt(path, url="http://127.0.0.1:8082/transcribe") -> (text, lang)`:调 8082,语种自动检测。
  - `translate(text, src_lang, url="http://127.0.0.1:8080/v1/chat/completions") -> str`:Qwen3.6 译中文;`src_lang` 已是 `zh` 则原样返回。
  - `is_noise(text) -> bool`:复用 chat.py 的幻觉/空串过滤,True=丢弃。

- [ ] **Step 1: 写失败测试**

```python
# ~/voice-assistant/tests/test_caption_core.py
# /// script
# requires-python = ">=3.10"
# dependencies = ["webrtcvad", "numpy"]
# ///
import sys, os, math, struct
sys.path.insert(0, os.path.expanduser("~/voice-assistant"))
import numpy as np, webrtcvad
import caption_core as cc

def _tone_frame(ms=30, sr=16000, freq=300):
    n = sr * ms // 1000
    t = np.arange(n)
    pcm = (0.3 * 32767 * np.sin(2*math.pi*freq*t/sr)).astype("<i2").tobytes()
    return pcm

def _silence_frame(ms=30, sr=16000):
    return b"\x00\x00" * (sr * ms // 1000)

def test_segment_splits_on_silence():
    # 1s 语音 + 1s 静音 + 0.6s 语音 → 期望切出 2 段(末段>min_speech)
    frames = [_tone_frame()]*33 + [_silence_frame()]*33 + [_tone_frame()]*20
    vad = webrtcvad.Vad(2)
    segs = list(cc.segment_frames(iter(frames), vad))
    assert len(segs) == 2, f"got {len(segs)} segs"
    assert all(isinstance(s, bytes) and len(s) > 0 for s in segs)

def test_segment_drops_too_short():
    # 只有 0.1s 语音(< min_speech 0.4s) → 0 段
    frames = [_tone_frame()]*3 + [_silence_frame()]*33
    segs = list(cc.segment_frames(iter(frames), webrtcvad.Vad(2)))
    assert segs == [], segs

def test_is_noise_filters_empty_and_hallu():
    assert cc.is_noise("") is True
    assert cc.is_noise("谢谢观看") is True
    assert cc.is_noise("Let's review the roadmap") is False

def test_translate_passthrough_zh():
    assert cc.translate("你好", "zh") == "你好"

if __name__ == "__main__":
    test_segment_splits_on_silence(); test_segment_drops_too_short()
    test_is_noise_filters_empty_and_hallu(); test_translate_passthrough_zh()
    print("OK")
```

- [ ] **Step 2: 跑测试确认失败**

Run: `cd ~/voice-assistant && uv run tests/test_caption_core.py`
Expected: FAIL — `No module named caption_core`。

- [ ] **Step 3: 写 caption_core.py**

```python
# ~/voice-assistant/caption_core.py
"""实时字幕的纯逻辑:VAD 切句 / STT / 翻译 / 幻觉过滤。不碰 GUI、不直接开音频设备。"""
import json, re, wave, urllib.request
from collections import Counter

SAMPLE_RATE = 16000
FRAME_MS = 30

HALLU_PHRASES = ["点赞", "订阅", "转发", "打赏", "字幕", "明镜", "点点栏目",
                 "感谢观看", "谢谢观看", "谢谢大家", "下期再见", "志愿者",
                 "请不吝", "关注我", "Amara", "字幕组"]
_HALLU_EXACT = {"字幕", "谢谢观看", "请订阅", "by", "you", ".", "", "thank you", "thanks"}


def segment_frames(frames, vad, sample_rate=SAMPLE_RATE, frame_ms=FRAME_MS,
                   silence_tail=0.8, min_speech=0.4):
    """输入 30ms int16 PCM 帧迭代器,按停顿切句,产出每段拼接 PCM bytes(丢弃过短段)。"""
    buf, triggered, silence = [], False, 0.0
    for chunk in frames:
        speech = vad.is_speech(chunk, sample_rate)
        if not triggered:
            if speech:
                triggered = True; buf = [chunk]; silence = 0.0
        else:
            buf.append(chunk)
            if speech:
                silence = 0.0
            else:
                silence += frame_ms / 1000
                if silence >= silence_tail:
                    dur = len(buf) * frame_ms / 1000
                    if dur >= min_speech:
                        yield b"".join(buf)
                    buf, triggered, silence = [], False, 0.0
    if triggered and len(buf) * frame_ms / 1000 >= min_speech:
        yield b"".join(buf)


def pcm_to_wav(pcm, path, sample_rate=SAMPLE_RATE):
    with wave.open(path, "wb") as w:
        w.setnchannels(1); w.setsampwidth(2); w.setframerate(sample_rate)
        w.writeframes(pcm)


def stt(path, url="http://127.0.0.1:8082/transcribe"):
    body = json.dumps({"path": path, "language": "auto"}).encode()
    req = urllib.request.Request(url, data=body, headers={"Content-Type": "application/json"})
    with urllib.request.urlopen(req, timeout=30) as r:
        d = json.load(r)
    return d.get("text", "").strip(), d.get("language", "")


_TRANS_SYS = ("你是同声传译。把用户给的这句话翻成简洁、口语化的简体中文,"
              "只输出译文本身,不要解释、不要原文、不要引号。")


def translate(text, src_lang, url="http://127.0.0.1:8080/v1/chat/completions"):
    if src_lang == "zh" or not text:
        return text
    body = json.dumps({
        "model": "qwen3.6", "max_tokens": 200, "temperature": 0.2,
        "repetition_penalty": 1.1,
        "chat_template_kwargs": {"enable_thinking": False},
        "messages": [{"role": "system", "content": _TRANS_SYS},
                     {"role": "user", "content": text}],
    }).encode()
    req = urllib.request.Request(url, data=body, headers={"Content-Type": "application/json"})
    with urllib.request.urlopen(req, timeout=60) as r:
        out = json.load(r)
    zh = (out["choices"][0]["message"].get("content") or "").strip()
    zh = re.sub(r"<think>.*?</think>", "", zh, flags=re.S).strip()
    return zh


def is_noise(text):
    """空串 / whisper 幻觉 / 字幕台词 → True(丢弃)。"""
    c = text.strip("。.，,！!？? ").lower()
    if not c or c in _HALLU_EXACT or len(c) < 2:
        return True
    if any(p in text for p in HALLU_PHRASES):
        return True
    t = re.sub(r"[，。！？、\s,.!?]", "", text)
    if len(t) >= 4:
        if Counter(t).most_common(1)[0][1] >= max(5, len(t) * 0.5):
            return True
    return False
```

- [ ] **Step 4: 跑测试确认通过**

需要 8080 大脑在跑(`llm start`)以测 translate 透传(zh 不调网络,故该用例离线也过;其余离线)。
Run: `cd ~/voice-assistant && uv run tests/test_caption_core.py`
Expected: PASS（打印 OK）。

---

## Task 3: caption_overlay — 底部双语字幕浮窗(pyobjc)

**Files:**
- Create: `~/voice-assistant/caption_overlay.py`

**Interfaces:**
- Produces: 类 `CaptionOverlay`:
  - `__init__()`:建无边框、置顶、点击穿透、半透明、底部居中的 NSPanel。
  - `update(orig: str, zh: str)`:刷新两行文字(必须在主线程调)。
  - `show()` / `hide()`。
- GUI 无法自动断言,**手动验证**。

- [ ] **Step 1: 写 caption_overlay.py**

```python
# ~/voice-assistant/caption_overlay.py
"""底部双语字幕浮窗:无边框、置顶、点击穿透、不抢焦点、半透明。"""
from AppKit import (
    NSPanel, NSColor, NSTextField, NSView, NSScreen, NSFont,
    NSWindowStyleMaskBorderless, NSWindowStyleMaskNonactivatingPanel,
    NSScreenSaverWindowLevel, NSBackingStoreBuffered,
    NSWindowCollectionBehaviorCanJoinAllSpaces,
    NSWindowCollectionBehaviorStationary, NSWindowCollectionBehaviorFullScreenAuxiliary,
)
from Foundation import NSMakeRect


class CaptionOverlay:
    def __init__(self):
        scr = NSScreen.mainScreen().frame()
        w, h = scr.size.width * 0.8, 96
        x = (scr.size.width - w) / 2
        y = 60  # 距屏幕底部
        rect = NSMakeRect(x, y, w, h)
        style = NSWindowStyleMaskBorderless | NSWindowStyleMaskNonactivatingPanel
        panel = NSPanel.alloc().initWithContentRect_styleMask_backing_defer_(
            rect, style, NSBackingStoreBuffered, False)
        panel.setLevel_(NSScreenSaverWindowLevel)        # 盖住会议/全屏
        panel.setOpaque_(False)
        panel.setBackgroundColor_(NSColor.clearColor())
        panel.setIgnoresMouseEvents_(True)               # 点击穿透
        panel.setHidesOnDeactivate_(False)
        panel.setCollectionBehavior_(
            NSWindowCollectionBehaviorCanJoinAllSpaces
            | NSWindowCollectionBehaviorStationary
            | NSWindowCollectionBehaviorFullScreenAuxiliary)

        bg = NSView.alloc().initWithFrame_(NSMakeRect(0, 0, w, h))
        bg.setWantsLayer_(True)
        bg.layer().setBackgroundColor_(NSColor.colorWithCalibratedWhite_alpha_(0.0, 0.55).CGColor())
        bg.layer().setCornerRadius_(14.0)
        panel.setContentView_(bg)

        self.orig = self._label(NSMakeRect(16, h/2, w-32, h/2-8), 18,
                                NSColor.colorWithCalibratedWhite_alpha_(0.85, 1.0))
        self.zh = self._label(NSMakeRect(16, 6, w-32, h/2-2), 26, NSColor.whiteColor())
        bg.addSubview_(self.orig); bg.addSubview_(self.zh)
        self.panel = panel

    def _label(self, frame, size, color):
        f = NSTextField.alloc().initWithFrame_(frame)
        f.setBezeled_(False); f.setEditable_(False); f.setSelectable_(False)
        f.setDrawsBackground_(False); f.setTextColor_(color)
        f.setFont_(NSFont.systemFontOfSize_(size))
        f.setStringValue_("")
        return f

    def update(self, orig, zh):
        self.orig.setStringValue_(orig or "")
        self.zh.setStringValue_(zh or "")

    def show(self):
        self.panel.orderFrontRegardless()

    def hide(self):
        self.panel.orderOut_(None)
```

- [ ] **Step 2: 手动验证浮窗**

Run:
```bash
cd ~/voice-assistant && uv run --with pyobjc-framework-Cocoa python3 -c "
from AppKit import NSApplication, NSApp
import caption_overlay, Foundation
app = NSApplication.sharedApplication()
o = caption_overlay.CaptionOverlay(); o.show()
o.update('So let us review the Q3 roadmap', '那我们来过一下三季度路线图')
# 2 秒后退出
Foundation.NSTimer.scheduledTimerWithTimeInterval_repeats_block_(2.0, False, lambda t: NSApp().terminate_(None))
app.run()
"
```
Expected(手动看):屏幕底部居中出现半透明黑条,上行英文小字、下行中文大字;**鼠标能点穿**(点字幕区域会点到下面的窗口);不抢焦点;2 秒后消失。

---

## Task 4: caption.py — 编排器(采集 + 流水线 + 浮窗 + 输出切换)

**Files:**
- Create: `~/voice-assistant/caption.py`
- Test: `~/voice-assistant/tests/test_caption_pipeline.py`

**Interfaces:**
- Consumes: caption_core(segment/stt/translate/is_noise/pcm_to_wav)、caption_overlay.CaptionOverlay。
- Produces: 可执行 `uv run caption.py`;内部 `process_segment(pcm) -> (orig, zh)|None`(供测试)。

- [ ] **Step 1: 写流水线测试(stub 网络,验证编排)**

```python
# ~/voice-assistant/tests/test_caption_pipeline.py
# /// script
# requires-python = ">=3.10"
# dependencies = ["sounddevice", "webrtcvad", "numpy", "pyobjc-framework-Cocoa"]
# ///
import sys, os
sys.path.insert(0, os.path.expanduser("~/voice-assistant"))
import caption_core as cc
import caption

def test_process_segment_translates(monkeypatch=None):
    caption.cc.stt = lambda path, **k: ("Let's review the roadmap", "en")
    caption.cc.translate = lambda text, lang, **k: "我们来看下路线图"
    out = caption.process_segment(b"\x00\x00" * 8000)
    assert out == ("Let's review the roadmap", "我们来看下路线图"), out

def test_process_segment_drops_noise():
    caption.cc.stt = lambda path, **k: ("谢谢观看", "zh")
    assert caption.process_segment(b"\x00\x00" * 8000) is None

if __name__ == "__main__":
    test_process_segment_translates(); test_process_segment_drops_noise(); print("OK")
```

- [ ] **Step 2: 跑测试确认失败**

Run: `cd ~/voice-assistant && uv run tests/test_caption_pipeline.py`
Expected: FAIL — `No module named caption` 或 `process_segment` 未定义。

- [ ] **Step 3: 写 caption.py**

```python
# ~/voice-assistant/caption.py
# /// script
# requires-python = ">=3.10"
# dependencies = ["sounddevice", "webrtcvad", "numpy", "pyobjc-framework-Cocoa"]
# ///
"""实时字幕编排器:采 BlackHole → VAD 切句 → whisper 转写 → Qwen3.6 译中文 → 底部浮窗。
只读 BlackHole(远端声音),不碰麦。启动切系统输出到「会议外放」,停止/异常切回。"""
import os, queue, tempfile, threading, subprocess

import numpy as np
import sounddevice as sd
import webrtcvad
from AppKit import NSApplication, NSApp
from Foundation import NSTimer

import caption_core as cc
import caption_overlay

IN_DEV = "BlackHole 2ch"
MONITOR_OUTPUT = "会议外放"
FALLBACK_OUTPUT = "MacBook Pro Speakers"
SAMPLE_RATE = 16000
FRAME = SAMPLE_RATE * 30 // 1000   # 480 samples / 30ms

ENV = {**os.environ, "PATH": "/opt/homebrew/bin:" + os.path.expanduser("~/.local/bin") + ":" + os.environ.get("PATH", "")}
_tmp = os.path.join(tempfile.gettempdir(), "caption_seg.wav")


def process_segment(pcm):
    """一段 PCM → (原文, 中文) 或 None(噪音/空)。"""
    cc.pcm_to_wav(pcm, _tmp)
    text, lang = cc.stt(_tmp)
    if cc.is_noise(text):
        return None
    zh = cc.translate(text, lang)
    if not zh:
        return None
    return (text, zh)


def get_output():
    try:
        r = subprocess.run(["SwitchAudioSource", "-c", "-t", "output"],
                           capture_output=True, text=True, env=ENV, timeout=8)
        return r.stdout.strip() or None
    except Exception:
        return None


def set_output(name):
    subprocess.run(["SwitchAudioSource", "-s", name, "-t", "output"],
                   capture_output=True, text=True, env=ENV, timeout=8)


def _find_input_index(name):
    for i, d in enumerate(sd.query_devices()):
        if d["max_input_channels"] > 0 and name in d["name"]:
            return i, d["max_input_channels"]
    raise RuntimeError(f"找不到输入设备「{name}」")


class Caption:
    def __init__(self):
        self.overlay = caption_overlay.CaptionOverlay()
        self.q = queue.Queue()          # 主线程要显示的 (orig, zh)
        self.frames = queue.Queue()     # 采集帧
        self.pending = queue.Queue()    # 切好的句子 → worker（在 __init__ 建好，避免两线程各建一个）
        self.running = True
        self.prev_output = None

    def start(self):
        # 切系统输出到 会议外放(远端声音才进 BlackHole;不碰麦)
        self.prev_output = get_output() or FALLBACK_OUTPUT
        set_output(MONITOR_OUTPUT)
        idx, ch = _find_input_index(IN_DEV)
        self.dev_ch = ch
        self.overlay.show()
        threading.Thread(target=self._capture, args=(idx, ch), daemon=True).start()
        threading.Thread(target=self._worker, daemon=True).start()
        # 主线程定时器:把后台算好的字幕刷到浮窗(GUI 必须主线程)
        NSTimer.scheduledTimerWithTimeInterval_repeats_block_(
            0.2, True, lambda t: self._drain())

    def _capture(self, idx, ch):
        def cb(indata, frames, t, status):
            a = np.frombuffer(bytes(indata), dtype="<i2")
            if ch > 1:                  # 多声道 → 取均值降单声道
                a = a.reshape(-1, ch).mean(axis=1).astype("<i2")
            self.frames.put(a.tobytes())
        with sd.RawInputStream(samplerate=SAMPLE_RATE, blocksize=FRAME,
                               dtype="int16", channels=ch, device=idx, callback=cb):
            vad = webrtcvad.Vad(2)
            def frame_iter():
                while self.running:
                    yield self.frames.get()
            for pcm in cc.segment_frames(frame_iter(), vad):
                if not self.running:
                    break
                self.pending.put(pcm)

    def _worker(self):
        while self.running:
            try:
                pcm = self.pending.get(timeout=0.5)
            except queue.Empty:
                continue
            try:
                out = process_segment(pcm)
            except Exception as e:
                print("process error:", e); out = None
            if out:
                self.q.put(out)

    def _drain(self):
        latest = None
        while not self.q.empty():
            latest = self.q.get()
        if latest:
            self.overlay.update(latest[0], latest[1])

    def stop(self):
        self.running = False
        try:
            self.overlay.hide()
        finally:
            if self.prev_output:
                set_output(self.prev_output)   # 务必切回


def main():
    app = NSApplication.sharedApplication()
    cap = Caption()
    try:
        cap.start()
    except Exception as e:
        print("启动失败:", e)
        if getattr(cap, "prev_output", None):
            set_output(cap.prev_output)
        return
    import signal
    def _quit(*a):
        cap.stop(); NSApp().terminate_(None)
    signal.signal(signal.SIGINT, _quit)
    signal.signal(signal.SIGTERM, _quit)
    try:
        app.run()
    finally:
        cap.stop()


if __name__ == "__main__":
    main()
```

注:采集线程把切好的句子放 `self.pending`,`_worker` 消费;`process_segment` 是独立模块级函数,测试直接 stub `caption.cc.stt/translate` 即可。

- [ ] **Step 4: 跑流水线测试确认通过**

Run: `cd ~/voice-assistant && uv run tests/test_caption_pipeline.py`
Expected: PASS（打印 OK）。

- [ ] **Step 5: 手动整链验证(真开一段外语音频)**

前置:`llm start`(8080)、stt_server 在跑(8082)。
Run: `cd ~/voice-assistant && uv run caption.py`,然后在浏览器放一段英文视频(或 `say "let us review the third quarter roadmap and the budget"` 经会议外放)。
Expected(手动看):系统输出被切到「会议外放」(你仍听得见);说一句英文,约 1-3 秒底部浮窗出现英文原文 + 中文译文;`Ctrl+C` 后浮窗消失、**系统输出切回原设备**。

---

## Task 5: CLI `caption` 启动器(检服务 + 拉起)

**Files:**
- Create: `~/.local/bin/caption`

**Interfaces:**
- Produces: 命令 `caption`,前台跑实时字幕,Ctrl+C 停。

- [ ] **Step 1: 写 ~/.local/bin/caption**

```bash
#!/bin/bash
# 实时字幕(外语→中文):采 BlackHole → whisper → Qwen3.6 → 底部双语浮窗。Ctrl+C 停。
export PATH="/opt/homebrew/bin:$HOME/.local/bin:$PATH"

# 大脑(8080)
if ! curl -s --max-time 2 http://127.0.0.1:8080/v1/models 2>/dev/null | grep -q qwen3.6; then
  echo "▶ 启动本地大脑…"; llm start; sleep 2
fi
# STT(8082)
if ! curl -s --max-time 2 -X POST http://127.0.0.1:8082/transcribe -H 'Content-Type: application/json' -d '{}' >/dev/null 2>&1; then
  echo "▶ 启动 STT 服务…"
  ( cd "$HOME/voice-assistant" && nohup uv run stt_server.py >/tmp/stt.log 2>&1 & )
  n=0; until curl -s --max-time 2 -X POST http://127.0.0.1:8082/transcribe -H 'Content-Type: application/json' -d '{}' >/dev/null 2>&1 || [ $n -ge 30 ]; do sleep 2; n=$((n+1)); done
fi
echo "🌐 实时字幕启动中(说外语→底部出中文)… Ctrl+C 退出"
exec uv run "$HOME/voice-assistant/caption.py"
```

- [ ] **Step 2: 赋权 + 验证**

Run: `chmod +x ~/.local/bin/caption && bash -n ~/.local/bin/caption && echo OK`
Expected: `OK`。再 `caption`(手动):服务自动拉起,浮窗出现,外语→中文,Ctrl+C 退出后输出切回。

---

## Task 6: 菜单栏 app 集成「🌐 实时字幕」开/关

**Files:**
- Modify: `~/voice-assistant/meeting_app.py`(`_idle` 菜单 + 新增方法)
- Create: `~/Library/LaunchAgents/com.local.caption.plist`
- Modify: `~/voice-assistant/meeting_app_launch.sh` 思路复用(caption 也经 launchd 启,规避 applet/进程回收)

**Interfaces:**
- Consumes: launchd 服务 `com.local.caption`(跑 `caption.py`)。
- Produces: 菜单项「🌐 实时字幕」点一下启动、再点停止;状态用 `launchctl print` 判断。

- [ ] **Step 1: 写 caption 的 LaunchAgent**

```bash
cat > ~/Library/LaunchAgents/com.local.caption.plist <<'P'
<?xml version="1.0" encoding="UTF-8"?>
<!DOCTYPE plist PUBLIC "-//Apple//DTD PLIST 1.0//EN" "http://www.apple.com/DTDs/PropertyList-1.0.dtd">
<plist version="1.0"><dict>
  <key>Label</key><string>com.local.caption</string>
  <key>ProgramArguments</key>
  <array>
    <string>/bin/bash</string><string>-c</string>
    <string>export PATH="/opt/homebrew/bin:$HOME/.local/bin:$PATH"; exec uv run "$HOME/voice-assistant/caption.py"</string>
  </array>
  <key>RunAtLoad</key><false/>
  <key>KeepAlive</key><false/>
  <key>ProcessType</key><string>Interactive</string>
  <key>StandardOutPath</key><string>__HOME__/.caption.log</string>
  <key>StandardErrorPath</key><string>__HOME__/.caption.log</string>
</dict></plist>
P
python3 -c "import plistlib;plistlib.load(open(os.path.expanduser('~/Library/LaunchAgents/com.local.caption.plist'),'rb'));print('OK')" 2>/dev/null || python3 -c "import plistlib,os;plistlib.load(open(os.path.expanduser('~/Library/LaunchAgents/com.local.caption.plist'),'rb'));print('OK')"
```

前置:菜单 app 跑 caption 需 8080/8082 已起;LaunchAgent 不自动起服务,故菜单项点击前在 app 里检测(下step)。

- [ ] **Step 2: 改 meeting_app.py — 菜单项 + 启停方法**

在 `_idle()` 的菜单里,「📂 打开录音文件夹」之前插入字幕开关项:

```python
    def _idle(self):
        self.title = "🎙️"
        self.menu.clear()
        cap_on = self._caption_running()
        self.menu = [
            rumps.MenuItem("🔴 录线上会议", callback=self.rec_online),
            rumps.MenuItem("🎤 录麦克风（线下）", callback=self.rec_mic),
            None,
            rumps.MenuItem("⏹ 停止实时字幕" if cap_on else "🌐 实时字幕(外语→中文)",
                           callback=self.toggle_caption),
            None,
            rumps.MenuItem("📂 打开录音文件夹", callback=self.open_folder),
            rumps.MenuItem("退出", callback=self.quit_app),
        ]
```

在类里新增:

```python
    CAPTION_SVC = "com.local.caption"

    def _caption_running(self):
        import subprocess
        r = subprocess.run(["launchctl", "print", f"gui/{os.getuid()}/{self.CAPTION_SVC}"],
                           capture_output=True, text=True, env=ENV)
        return "state = running" in r.stdout

    def toggle_caption(self, _):
        import subprocess
        uid = os.getuid()
        svc = f"gui/{uid}/{self.CAPTION_SVC}"
        plist = os.path.expanduser("~/Library/LaunchAgents/com.local.caption.plist")
        if self._caption_running():
            subprocess.run(["launchctl", "kill", "TERM", svc], env=ENV)
        else:
            # 前置:大脑+STT。大脑没起就提示(不阻塞 UI)
            ok = subprocess.run(["bash", "-lc",
                "curl -s --max-time 2 http://127.0.0.1:8080/v1/models | grep -q qwen3.6"],
                env=ENV).returncode == 0
            if not ok:
                rumps.alert("大脑未就绪", "先在终端 `llm start`,再开实时字幕。"); return
            subprocess.run(["launchctl", "bootstrap", f"gui/{uid}", plist],
                           capture_output=True, env=ENV)
            subprocess.run(["launchctl", "kickstart", svc], capture_output=True, env=ENV)
        self._idle()   # 刷新菜单文案
```

需在文件顶部确保 `import subprocess`(已存在)。

- [ ] **Step 3: 编译 + 手动验证**

Run: `python3 -m py_compile ~/voice-assistant/meeting_app.py && echo OK`
Expected: `OK`。
手动:`llm start` + stt_server 起;`launchctl kickstart -k gui/$(id -u)/com.local.meeting-app` 重载菜单 app;点菜单「🌐 实时字幕」→ 浮窗出现、菜单文案变「⏹ 停止实时字幕」;再点 → 浮窗消失、输出切回。

---

## Task 7: 收尾验证(整体冒烟 + 输出切回兜底)

**Files:** 无新增,跑全链验证。

- [ ] **Step 1: 输出切回兜底测试(模拟异常)**

```python
# 临时验证:caption.Caption.start 抛错也要切回
cd ~/voice-assistant && uv run --with sounddevice --with webrtcvad --with numpy --with pyobjc-framework-Cocoa python3 -c "
import caption
orig = caption.get_output()
# 把输入设备名改坏 → start 抛错 → main 的 except 应切回
caption.IN_DEV = '不存在的设备'
import AppKit; AppKit.NSApplication.sharedApplication()
cap = caption.Caption()
try: cap.start()
except Exception as e: print('预期失败:', e)
cap.stop()
print('输出已切回:', caption.get_output() == orig)
"
```
Expected:打印「预期失败」+「输出已切回: True」。

- [ ] **Step 2: 全部自动化测试过一遍**

Run:
```bash
cd ~/voice-assistant
uv run tests/test_stt_lang.py && uv run tests/test_caption_core.py && uv run tests/test_caption_pipeline.py
```
Expected:三个都打印 OK。

- [ ] **Step 3: 真·会议手动验证**

开一场英文 Google Meet / 放英文视频 → 菜单「🌐 实时字幕」→ 底部出双语字幕、麦克风在 Meet 里仍可用(没抢麦)→ 关掉 → 输出切回。记录延迟体感(目标 1-3 秒/句)。
