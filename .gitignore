# Python
__pycache__/
*.py[cod]
.venv*/
*.egg-info/

# 备份 / 临时
*.bak
*.orig
.DS_Store

# 运行时产物(录音、转写、日志、证书)
*.log
*.wav
*.m4a
*.mp3
*.pem
*.crt
*.key
会议录音/
转写_*/

# 本地配置(含内网地址与 key)
*.env
config.local.sh

# 线上 web/ 的凭据与证书(从服务器同步时必须排除)
secrets.env
hf.env
cf.ini

# 声纹是生物特征:代码要可追溯、这类数据要可删除,生命周期相反,绝不入库
voiceprints.json
*.embeddings.npz

# Playwright
node_modules/
test-results/
playwright-report/
